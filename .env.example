# Copy this file to .env and fill in real keys.
#   cp .env.example .env  &&  chmod 600 .env
#
# Required only when --propose-fixes is used. Sweep-only runs need no keys.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
